import React from 'react';
import {AppRegistry, Text, View, TextInput, TouchableOpacity, Alert, Image, ScrollView} from 'react-native';
import Route from './route/navigation'

export default class App extends React.Component {
  render() {
    return (
        <Route/>
    )
  }
}